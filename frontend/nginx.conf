server {
    listen 8080;

    location /api/ {
        proxy_pass https://proiect-tweb-production.up.railway.app;
        proxy_set_header Host proiect-tweb-production.up.railway.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_ssl_server_name on;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
